import Hero from './Hero';

const Home = () => {
  return (
    <div className="min-h-screen bg-gradient-to-l from-slate-50 via-gray-50 to-gray-100">
      {/* Hero Section with Circular Diagram */}
      <Hero />
    </div>
  );
};

export default Home;
